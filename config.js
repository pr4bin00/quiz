/* ============================================================
   CONFIG — the only file you need to edit.
   Both index.html and scores.html read their settings from here.
   ============================================================ */

window.QUIZ_CONFIG = {

  /* 1. PASTE YOUR SCOREBOARD URL HERE.
        You get this from step A6 in README.md (it ends in /exec).
        Leave it as "" and each visitor will only ever see their own
        score — nothing is shared.                                  */
  endpoint: "",


  /* 2. Leave these alone unless you rename the files or split them
        across two different domains (see README.md, Part D).        */
  quizUrl:   "index.html",
  scoresUrl: "scores.html"

};
